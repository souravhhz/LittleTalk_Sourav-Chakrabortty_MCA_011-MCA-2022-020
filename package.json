{
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && node index.js",
    "socket": "cd socket && node index.js",
    "start": "npm-run-all --parallel socket server client"
  },
  "dependencies": {
    "react-router-dom": "^6.21.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
